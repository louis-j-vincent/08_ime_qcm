# IME QCM Generator (v0)

Tool to generate simple reading-comprehension QCMs from short French texts (IME-friendly).
- Input: text (<50 words)
- Output: editable QCM + PDF export

## Run
```bash
pip install -r requirements.txt
streamlit run app/app.py



quit
[200~
---

## 4) Environnement Python (isolé, obligatoire)
Sur macOS/Linux :
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
~
word
